import { LocaleString } from "../../../../../../core/core.module";

export class OrderServiceModel {
    Title: LocaleString = {
        "vi": ""
    };

    Query: string = "";

    ReceiveBy: string = "";

    AddFeatureToCart: boolean = false;
}